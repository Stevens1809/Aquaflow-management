<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GestionAquaFlow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorSurfaceVariant">@color/nav_bg_dark</item>
    </style>
    <style name="App.CustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:width">8dp</item>
        <item name="android:height">8dp</item>
        <item name="colorSecondaryContainer">@color/nav_active</item> <item name="shapeAppearance">@style/ShapeAppearance.Material3.Corner.Full</item> </style>
</resources>